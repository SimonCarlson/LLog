# LLOG
A LiftingLog to track your lifts. Teaching myself to use Rust.

## Run
`cargo run`

Built with rustc version 1.54.0

## Logging
Default log level is debug. This can be overwritten using the `RUST_LOG` env variable, for instance `RUST_LOG="info" cargo run`.
